config:
  target: 'http://localhost:5000'
  phases:
    - duration: 60
      arrivalRate: 1
      rampTo: 5
scenarios:
  - flow:
    - get:
        url: '/names'
    - get:
        url: '/titles'
    - get:
        url: '/filter?genre=Drama&rating=8&votes=100'